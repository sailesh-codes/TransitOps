You are a senior full-stack product engineer and UI systems designer. Build a production-ready web application from the problem statement provided below.

Important context:
- The actual problem statement will be provided below this prompt.
- You must first read the problem statement, extract the core product goal, target users, workflows, required entities, optional AI-assisted utilities, and key success metrics, then build the site around that.
- Think and build like a professional developer shipping a polished startup-grade product under real constraints, not like a template generator.

NON-NEGOTIABLE OUTPUT STANDARD
- The result must feel premium, fast, modern, and launch-ready.
- No generic AI-style layout patterns, no boilerplate-looking UI, no cheap gradients everywhere, no clutter, no uneven spacing, and no inconsistent visual treatment.
- Every section, card, modal, dropdown, tooltip, toast, empty state, skeleton loader, form state, and dashboard panel must feel like part of one cohesive design system.
- The site must have a clear wow factor while still being practical, readable, and performant.
- The site must be desktop-optimized and mobile-optimized from the start.

DESIGN DIRECTION
- Primary background: white.
- Accent palette: blue, red, and purple, used in a controlled and elegant way.
- The color usage must be consistent across the whole site; no random shades, no mismatched components, no uneven color density.
- Visual style: premium SaaS/product UI, refined spacing, strong typography hierarchy, subtle depth, tasteful motion, high clarity.
- Make the UI look handcrafted by an expert designer-developer.
- Avoid over-decoration; use motion and contrast intelligently.
- Ensure modals, toasts, popovers, side panels, tables, forms, and all micro-interactions match the same premium design language.

STRICT VISUAL RESTRICTIONS
- Do not create or use any AI-generated logo.
- Do not use emoji-based branding or decorative emojis.
- Do not use sparkles, thunderbolt icons, parcel icons, or similar cliché AI-generated visual motifs unless they are explicitly required by the problem statement for actual product meaning.
- Do not generate a logo automatically. If branding is needed, use a clean text-based wordmark or a simple developer-crafted geometric mark only if explicitly requested.
- Do not create a typical AI boilerplate hero section or generic “future of innovation” marketing copy.
- Avoid the common AI layout pattern of three identical feature cards in a row unless the product actually demands it.

STACK REQUIREMENTS
- JavaScript-based project.
- Framework: Next.js with React.
- Prefer App Router and production-ready patterns.
- Use Node.js only where required; otherwise use Next.js routes/server actions appropriately.
- Database target: MySQL.
- The codebase must already include MySQL-ready integration points, schemas, API contracts, field definitions, validation flow, and connection structure so I can plug in the actual database credentials later and use it directly.
- Keep DB integration clean and modular.
- If ORM is used, prefer Prisma with MySQL-ready schema and service structure.
- Frontend animation libraries may include GSAP, anime.js, or three.js, but use them only where genuinely beneficial. Motion must feel premium, smooth, and purposeful, not distracting.

SECURITY REQUIREMENTS
- Build database-related code with secure production patterns.
- Keep secrets server-side only; never expose secrets in client code.
- Never assume client input is trusted.
- Validate all input on the server using a clean schema validation layer.
- Add authentication/authorization placeholders where appropriate based on the problem statement.
- Protect all data mutation routes and server actions with explicit validation and authorization checks.
- Include clear environment variable placeholders for DB connection and secrets.
- Prepare code so environment variables are easy to wire later.
- Use least-privilege design for DB access and role-based logic where relevant.
- Add secure error handling and avoid leaking sensitive information in responses. These are consistent with current Next.js and Prisma security guidance. [web:1][web:6][web:8][web:15]

PROJECT STRUCTURE RULES
- Keep the file structure neat, readable, and not overly complex.
- Organize by feature where sensible, but do not over-engineer.
- Use clear and professional file/folder names.
- There must not be any file names using the forbidden platform term.
- If a platform-specific config file is necessary, use an alternate descriptive name that does not contain that forbidden term.
- Do not mention or create any file with that forbidden name anywhere in the project output.
- Keep the structure easy for a hackathon team to understand quickly.

PERFORMANCE REQUIREMENTS
- The site must feel fast and polished.
- Avoid unnecessary heavy dependencies.
- Use code splitting and lazy loading where appropriate.
- Optimize animations so they do not block rendering.
- Minimize layout shift.
- Use optimized image handling.
- Ensure the app does not feel buffered, laggy, or overloaded.
- Run production-grade performance-minded patterns: optimized builds, bundle awareness, and proper server/client separation. [web:1]

UX REQUIREMENTS
- Build complete product logic, not just screens.
- Define all main workflows neatly and correctly.
- Include loading states, empty states, success states, validation states, and error states.
- Forms must have clear validation and friendly inline feedback.
- Use clear information hierarchy and frictionless navigation.
- Mobile responsiveness must not be an afterthought; design it intentionally.
- Ensure touch targets, drawer behavior, forms, tables, and action controls are mobile-friendly.

AI/SMART FEATURES RULE
- Based on the problem statement, intelligently suggest and implement relevant high-value features such as:
  - chatbot,
  - summarization,
  - recommendations,
  - smart search,
  - analytics insights,
  - assistant workflows,
  - automated tagging,
  - natural language actions,
  - report generation.
- Only include these if they genuinely improve the product described in the problem statement.
- These features should feel product-native, not gimmicky.
- If any AI-assisted feature is included, the UI must still remain clean, professional, and not “AI-branded.”

IMPLEMENTATION REQUIREMENTS
1. First analyze the problem statement and derive:
   - product type,
   - target users,
   - main user journeys,
   - core pages,
   - data entities,
   - required dashboard/components,
   - security considerations,
   - optional premium features.

2. Then create:
   - a concise architecture plan,
   - route/page map,
   - reusable component plan,
   - database schema plan for MySQL,
   - API/server action plan,
   - state management approach,
   - styling/design system rules.

3. Then generate the actual project with:
   - production-minded Next.js structure,
   - reusable UI components,
   - consistent styling,
   - MySQL-ready backend code,
   - environment placeholders,
   - validation,
   - secure CRUD flows,
   - polished responsive UI.

4. Also include:
   - sample seed data or mock data layer for local preview,
   - clear placeholder values for DB credentials,
   - comments only where necessary,
   - easy replacement points for real DB wiring.

DATABASE EXPECTATIONS
- Define all likely MySQL tables/models required by the product.
- Include relationships, timestamps, enums/status fields, and indexes where relevant.
- Include server-side data access utilities.
- Include create/read/update/delete flow scaffolding as required by the problem statement.
- Include migration-ready schema structure.
- The DB layer should be “connect and use” after I add actual credentials.
- Do not hardcode real secrets.
- If Prisma is used, initialize MySQL datasource placeholders and include clean schema models and service usage patterns. Prisma’s documented Next.js integration supports this setup pattern. [web:13][web:15]

NEXT.JS QUALITY BAR
- Prefer App Router.
- Use server components where beneficial.
- Use client components only where interactivity requires them.
- Separate server-only code safely from client bundles.
- Keep environment variables private unless they are explicitly public.
- Validate production readiness through build-safe patterns and clean deployment assumptions. [web:1][web:6]

UI DETAIL EXPECTATIONS
- Use a strong type scale and spacing rhythm.
- Use premium buttons, inputs, tables, cards, tabs, dialogs, sheets, and notifications.
- Use tasteful shadows, borders, and focus states.
- Keep accessibility in mind: semantic structure, keyboard interactions, visible focus, readable contrast.
- Make all overlays and transient UI feel exceptionally well-designed.
- All pages should feel visually connected as one system.

ANIMATION EXPECTATIONS
- Use animations only when they improve clarity or delight.
- Use subtle entrance transitions, smooth state changes, polished hover/press states, and premium page feel.
- Avoid flashy motion spam.
- If using GSAP/anime.js/three.js, keep it intentional and lightweight.

CONTENT EXPECTATIONS
- Do not use generic filler copy.
- Generate content and labels that fit the actual problem statement.
- Keep copy clear, specific, and product-relevant.

DELIVERABLE EXPECTATIONS
- Produce a complete working project scaffold, not a vague prototype.
- Ensure the generated code is neat, readable, and maintainable.
- Keep naming professional and consistent.
- Keep the architecture simple enough for hackathon speed, but polished enough to feel investor-demo ready and production-minded.

Before generating the final codebase:
- Confirm the inferred feature set from the problem statement.
- Add useful extras only when they clearly support the product goal.
- Prioritize clarity, polish, speed, consistency, and strong engineering decisions.
- The final result must feel like it was built by an excellent developer with design taste.

PROBLEM STATEMENT: project name - TransitOps
Objective: Build an end-to-end transport operations platform that digitizes vehicle,
driver, dispatch, maintenance, and expense management while enforcing business
rules and providing operational insights.
Many logistics companies still rely on spreadsheets and manual logbooks to
manage their transport operations. This often leads to scheduling conflicts,
underutilized vehicles, missed maintenance, expired driver licenses, inaccurate
expense tracking, and poor operational visibility.
Your task is to build TransitOps, a centralized platform that allows organizations to
manage the complete lifecycle of their transport operations—from vehicle
registration and driver management to dispatching, maintenance, fuel logging, and
analytics.
Fleet Manager: Oversees fleet assets, maintenance, vehicle lifecycle, and
operational efficiency.
Driver: Creates trips, assigns vehicles and drivers, and monitors active deliveries.
Safety Officer: Ensures driver compliance, tracks license validity, and monitors
safety scores.
Financial Analyst: Reviews operational expenses, fuel consumption, maintenance
costs, and profitability.

Functional Requirement:

3.1 Authentication
● Implement secure login using email and password.
● Support Role-Based Access Control (RBAC).
● Only authenticated users should access the application.
3.2 Dashboard
● Display KPIs such as Active Vehicles, Available Vehicles, Vehicles in
Maintenance, Active Trips, Pending Trips, Drivers On Duty, and Fleet
Utilization (%).
● Provide filters by vehicle type, status, and region.
3.3 Vehicle Registry
● Maintain a master list of vehicles with Registration Number (unique), Vehicle
Name/Model, Type, Maximum Load Capacity, Odometer, Acquisition Cost,
and Status.
● Status values: Available, On Trip, In Shop, Retired.
3.4 Driver Management
● Maintain driver profiles including Name, License Number, License Category,
License Expiry Date, Contact Number, Safety Score, and Status.
● Status values: Available, On Trip, Off Duty, Suspended.
3.5 Trip Management
● Create trips by selecting a source, destination, available vehicle, available
driver, cargo weight, and planned distance.
● Trip lifecycle: Draft → Dispatched → Completed → Cancelled.
3.6 Maintenance
● Create maintenance records for vehicles.
● Adding a vehicle to a "Maintenance Log" automatically switches its status to
"In Shop", removing it from the Driver's selection pool.
3.7 Fuel & Expense Management
● Record fuel logs (liters, cost, date) and other expenses such as tolls or
maintenance.
● Automatically compute total operational cost (Fuel + Maintenance) per
vehicle.
3.8 Reports & Analytics
● Display Fuel Efficiency (Distance/Fuel), Fleet Utilization, Operational Cost,
and Vehicle ROI[\frac{Revenue - (Maintenance + Fuel)}{Acquisition Cost}].
● Support CSV export; PDF export is optional

Mandatory Business Rules:
The vehicle registration number must be unique.
● Retired or In Shop vehicles must never appear in the dispatch selection.
● Drivers with expired licenses or Suspended status cannot be assigned to trips.
● A driver or vehicle already marked On Trip cannot be assigned to another trip.
● Cargo Weight must not exceed the vehicle's maximum load capacity.
● Dispatching a trip automatically changes both the vehicle and driver status to
On Trip.
● Completing a trip automatically changes both the vehicle and driver status back
to Available.
● Cancelling a dispatched trip restores the vehicle and driver to Available.
● Creating an active maintenance record automatically changes vehicle status to
In Shop.
● Closing maintenance restores the vehicle to Available (unless retired).

Example Workflow:
Step 1: Register a vehicle 'Van-05' with a maximum capacity of 500 kg. Status =
Available.
Step 2: Register driver 'Alex' with a valid driving license.
Step 3: Create a trip with Cargo Weight = 450 kg.
Step 4: System validates that 450 kg ≤ 500 kg and allows dispatch.
Step 5: Vehicle and Driver status automatically become On Trip.
Step 6: Complete the trip by entering the final odometer and fuel consumed.
Step 7: System marks both Vehicle and Driver as Available.
Step 8: Create a maintenance record (e.g., Oil Change). Vehicle status
automatically becomes In Shop and is hidden from dispatch.
Step 9: Reports update operational cost and fuel efficiency based on the latest trip
and fuel log.

Expected Database Entities:
sers, Roles, Vehicles, Drivers, Trips, Maintenance Logs, Fuel Logs, Expenses

Mandatory Deliverables:
Responsive web interface
● Authentication with RBAC
● CRUD for Vehicles and Drivers
● Trip Management with validations
● Automatic status transitions
● Maintenance workflow
● Fuel & Expense tracking
● Dashboard with KPIs

Bonus Features:
Charts and visual analytics
● PDF export
● Email reminders for expiring licenses
● Vehicle document management
● Search, filters, and sorting
● Dark mode